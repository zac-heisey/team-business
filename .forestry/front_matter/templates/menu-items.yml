---
label: Menu Items
hide_body: true
fields:
- type: field_group_list
  name: items
  label: Items
  fields:
  - type: text
    name: title
    label: Title
  - type: text
    name: url
    label: URL
    description: The URL of the menu item
  - type: number
    name: weight
    label: Weight
    description: The order of the menu item in your menu
  - name: sub-items
    type: field_group_list
    fields:
    - name: title
      type: text
      config:
        required: false
      label: Title
    - name: url
      type: text
      config:
        required: false
      label: URL
    - name: weight
      type: number
      label: Weight
      description: ''
      default: 0
      required: true
      config:
        min: 
        max: 
        step: 
    config:
      min: 
      max: 
      labelField: 
    label: Sub-items
    description: Menu sub-items for the current menu item
